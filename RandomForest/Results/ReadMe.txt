#Struktur der Dateinamen:

RF_iiiiTree_jjVars_Phone_Median.RData


#Erklärung der Struktur

iiii <- Anzahl der Trees
jj   <- Anszahl der Variablen

Phone im Namen       <- Variable Phone NICHT in Bins
Phone nicht im Namen <- Variable Phone IN Bins

Median/Mean im Namen       <- Datensatz mit Median/Mean Imputation wurde verwendet
Median/Mean nicht im Namen <- Datensatz ohne Imputation wurde verwendet (na.omit)